# Dependencies
node_modules/

# Build output / Slidev / Vite
dist/
.slidev/
*.local

# Cloudflare Wrangler local state (wrangler dev / deploy)
.wrangler/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp

# Logs / scratch
*.log

# scripts/ local dev tooling — self-bootstrapped deps + screenshot output
# (node_modules/ already ignored above; these are the remaining artifacts)
scripts/package-lock.json
scripts/.shots/
